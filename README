 ===================================================================================
                             同步小程序使用说明
 1.基本信息
     主程序脚本：synchronous.py
     配置文件：Config.xlsx
     基本功能：实现数据库A向数据B同步数据，通过修改配置文件，定制任务。主要实现增量/
 全量同步，自主选择同步表，多任务队列。
	 数据库类型：Mysql
	 
 2.如何使用
     在配置文件中，填写A/B数据库账户信息以及同步规则，运行脚本即可同步数据库。
     配置文件说明如下：
         source：源数据库信息，待迁移数据库A。
         target：目标数据库信息，迁移目标数据库B。
         host：数据库主机IP
         user：数据库账号
         password：相应数据库账号的密码
         database：源数据库A，目标数据库B
         start_time：crontab定时计划，自动配置同步时间（待实现）
         mode：同步模式，默认为覆盖模式（replace），删除源库同名表，全量同步，插入模
             式（append)，增量同步，且增量同步可以传入"key='value'"参数进main方法做到
			 对同步表内容的控制
         filter：过滤符合正则条件的数据表，不进行同步
         choose：制定符合正则条件的数据表，进行同步
         switch：任务开关，当且仅当值为“on”时，任务启动

 3.注意事项
     a) 请勿修改配置文件表格表头。
     b) 同步模式默认为覆盖模式，请注意数据备份。
     b) filter以及choose功能，多正则输入使用“\”分隔符，正则条件无需引号。
     c) 当filter以及choose均为非空时，默认启动贪婪模式及先执行filter条件，再执行
         choose条件，非贪婪则相反。模式切换通过传入match参数控制
	 e) 同步顺序由配置文件中sheet顺序决定
	 
 ===================================================================================
